---
# Скопируйте в all_secret.yml и задайте пароль. Файл all_secret.yml в .gitignore.
# cp all_secret.example.yml all_secret.yml
# Альтернатива: export POSTGRES_PASSWORD='...' перед запуском ansible-playbook (или добавьте в .env и выполните source .env).

postgres_password: 'password'
